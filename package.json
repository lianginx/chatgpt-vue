{
  "name": "chatgpt-vue",
  "version": "0.0.1",
  "author": "LiangINX",
  "license": "MIT",
  "scripts": {
    "i": "yarn web:init && yarn server:init",
    "start": "cd web && yarn start && docker-compose up -d",
    "web:dev": "cd web && yarn dev",
    "web:build": "cd web && yarn build",
    "web:init": "cd web && yarn",
    "server:init": "cd server && yarn"
  },
  "dependencies": {},
  "devDependencies": {}
}